//处理时间格式问题
export function timeType(code) {
  if (!code) return '-';
  console.log('code=' + code);
  // for (var i = 0; i < code.length; i++) {
  //   code[i].create_time = code[i].create_time
  //     .split('T')
  //     .join(' ')
  //     .substring(0, 19);
  // }
  // return '-';
}
